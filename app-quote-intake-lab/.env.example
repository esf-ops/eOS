# Quote Intake Lab — local examples only. Copy to .env / .env.local (never commit secrets).

# Vite frontend (lab request token is NOT a Gemini credential)
VITE_QIL_LIVE_SERVER_URL=http://127.0.0.1:5197
VITE_QIL_LAB_REQUEST_TOKEN=replace-with-local-lab-token

# Optional head link
# VITE_HEAD_URL_HOME=https://www.eliteosfab.com
