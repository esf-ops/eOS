import React from "react";
import ReactDOM from "react-dom/client";
import AccountDirectoryApp from "./AccountDirectoryApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <AccountDirectoryApp />
  </React.StrictMode>
);
