VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_BACKEND_URL=http://localhost:3001
VITE_HEAD_URL_HOME=http://localhost:5177
# UI render gate only — never grants access. Backend pilot + head access are authoritative.
VITE_ELITE100_ESTIMATE_STUDIO_UI_ENABLED=false
# Configuration envelope builder UI only — never grants access. Backend pilot + flags are authoritative.
VITE_ELITE100_ESTIMATE_STUDIO_CONFIGURATION_UI_ENABLED=false
# Optional display of public portal origin (links still come from Brain publish response).
# VITE_HEAD_URL_DIGITAL_ESTIMATE=https://digital.eliteosfab.com
