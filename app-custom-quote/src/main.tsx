import React from "react";
import ReactDOM from "react-dom/client";
import CustomQuoteApp from "./CustomQuoteApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CustomQuoteApp />
  </React.StrictMode>
);
