/**
 * Customer-safe print HTML for email PDF attachment.
 * Rendered via shared CustomerEstimateDocument (React SSR bundle).
 */
export { buildCustomerEstimatePrintHtml } from "./generated/customerEstimateDocumentRender.mjs";
