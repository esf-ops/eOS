import React from "react";
import ReactDOM from "react-dom/client";
import InstallDashboardApp from "./InstallDashboardApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <InstallDashboardApp />
  </React.StrictMode>
);
