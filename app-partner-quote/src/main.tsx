import React from "react";
import ReactDOM from "react-dom/client";
import PartnerQuoteApp from "./PartnerQuoteApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PartnerQuoteApp />
  </React.StrictMode>
);
